Usage: $0 install <plugin(s)>

Description:

  Installs hoodie plugin(s)

Options:

  --plugins, -p <plugins>       single plugin name or comma separated list

Examples:

  $ $0 install
  $ $0 install users
  $ $0 install -p users
  $ $0 install -p users,shares,cheesecake
