Installs hoodie plugin(s).

Usage:
  $0 install
  $0 install <plugin>...
  $0 install -p <plugin>...
  
Options:
  -p | --plugins  Plugin(s) to be installed.
  -v | --verbose  Verbose output.

Arguments:
  <plugin>        Single plugin name or comma separated list.
